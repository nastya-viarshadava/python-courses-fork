"""
Task 03: Match on tuples with different structures.

Write a function `process_command(command) -> str` that takes a tuple and:
- If ("move", x, y), returns f"Moving to ({x}, {y})"
- If ("draw", "circle", radius), returns f"Drawing circle with radius {radius}"
- If ("draw", "rectangle", width, height), returns f"Drawing rectangle {width}x{height}"
- Otherwise, returns "Unknown command"
"""

def process_command(command) -> str:
    # TODO: Implement the function
    # Remove 'pass' and write your solution using match on tuples
    pass