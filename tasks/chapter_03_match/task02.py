"""
Task 02: Classify age group with match and guards.

Write a function `classify_age(age: int) -> str` that:
- Returns "Child" if age is less than 13
- Returns "Teenager" if age is between 13 and 19 inclusive
- Returns "Adult" if age is between 20 and 64 inclusive
- Returns "Senior" if age is 65 or greater
- For invalid ages (negative), return "Invalid age"
"""

def classify_age(age: int) -> str:
    # TODO: Implement the function
    # Remove 'pass' and write your solution using match with guards
    pass