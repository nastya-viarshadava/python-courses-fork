"""
Task 01: Count up to a number.

Write a function `count_up(n: int) -> list[int]` that:
- Takes an integer n
- Uses a `while` loop
- Returns a list containing numbers from 1 to n (inclusive)
"""

def count_up(n: int) -> list[int]:
    # TODO: Implement the function using a while loop
    pass