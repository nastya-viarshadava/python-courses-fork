"""
Task 08: Login attempts.

Write a function `login_attempts(attempts: list[str], correct: str, max_attempts: int) -> bool` that:
- Uses a `for` loop
- Allows up to `max_attempts` password attempts
- Returns True if the `correct` password is entered within the allowed attempts
- Returns False if all attempts fail or exceed the limit
- Must use a for–else loop

Hint
Use Python List Slicing to limit the attempts to max_attempts
"""

def login_attempts(attempts: list[str], correct: str, max_attempts: int) -> bool:
    # TODO: Implement the function using for–else and range
    pass