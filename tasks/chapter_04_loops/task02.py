"""
Task 02: Sum numbers until zero or less.

Write a function `sum_until_zero_or_less(numbers: list[int]) -> int` that:
- Uses a `while` loop
- Iterates over the given list of `numbers`
- Adds numbers to a total until a number that is 0 or less is encountered
- Stops processing when a number 0 or less is found
- Returns the calculated sum
"""

def sum_until_zero_or_less(numbers: list[int]) -> int:
    # TODO: Implement the function using a loop and break
    pass