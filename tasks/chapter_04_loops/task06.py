"""
Task 06: Even numbers.

Write a function `even_numbers(n: int) -> list[int]` that:
- Uses a `for` loop and `range`
- Takes an integer n
- Returns all even numbers between 0 and n (inclusive)
"""

def even_numbers(n: int) -> list[int]:
    # TODO: Implement the function using for and range
    pass