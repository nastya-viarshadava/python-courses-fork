"""
Task 03: Check password.

Write a function `check_password(attempts: list[str], correct: str) -> bool` that:
- Uses a `while` loop
- Iterates through password `attempts`
- Returns True if the `correct` password is found
- Stops checking after the first correct attempt
- Returns False if the password is never found
"""

def check_password(attempts: list[str], correct: str) -> bool:
    # TODO: Implement the function using a loop and break
    pass